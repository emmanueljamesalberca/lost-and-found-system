# --- Root ignores ---
.env
node_modules/
.DS_Store
.vscode/
.idea/

# If you also have a **top-level** uploads folder, ignore it
# (anchored so it won't match backend/uploads)
#/uploads/

# --- Backend ignores ---
backend/.env
backend/node_modules/
backend/certs/

# --- Keep backend/uploads tracked (empty except .gitkeep) ---
backend/uploads/*
!backend/uploads/.gitkeep
